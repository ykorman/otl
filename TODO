* invalidate password after a successful login
* find another popular web service for the cloud to demostrate with
* implement "make install" command
* expand on how to update PAM configuration
* don't see our syslog prefix in the journal
* support selinux properly
* add tests
** how do i test this? is there a test module for pam? maybe a docker image with
   custom linux-pam configuration to run tests against?.
* add 'check' target to makefile: run spellcheck, linter (clang?) and unit tests.
* add versioning
* add builddeps and test configuration to vagrant file
* use PAM_CONST where appropriate ?
* generate gh-pages website
* use travis or another online ci
* create a chrome app that can gets an ssh key and gets the otl password
  automatically (see maybe https://chromium.googlesource.com/apps/libapps/+/master/nassh/doc/FAQ.md)
* make a demo video
